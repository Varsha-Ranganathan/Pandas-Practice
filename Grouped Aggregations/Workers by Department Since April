# Import your libraries
import pandas as pd

workers_after_april = worker[worker.joining_date >= '2014-04-01']

workers_after_april.groupby(by='department').size().sort_values(ascending=False).reset_index()
