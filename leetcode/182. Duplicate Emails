import pandas as pd

def duplicate_emails(person: pd.DataFrame) -> pd.DataFrame:
    person["counts"] = person.groupby(by="email").email.transform("count")
    result = person[person.counts > 1][["email"]]
    return result.drop_duplicates()
