import pandas as pd

def combine_two_tables(person: pd.DataFrame, address: pd.DataFrame) -> pd.DataFrame:
    person_address_table = pd.merge(person, address, on="personId", how="left" )
    return person_address_table[["firstName", "lastName", "city", "state"]]
    
