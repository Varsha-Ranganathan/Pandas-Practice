# Import your libraries
import pandas as pd

customers_renamed = customers.rename(columns={"id": "cust_id"})
updated = pd.merge(customers_renamed, orders, how="left")
updated.loc[:, ["first_name","last_name", "city", "order_details"]].sort_values(["first_name", "order_details"])
