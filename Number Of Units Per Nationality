# Import your libraries
import pandas as pd

airbnb_hosts_under_30 = airbnb_hosts[(airbnb_hosts.age < 30)]
hosts_units_info = pd.merge(airbnb_hosts_under_30, airbnb_units, on="host_id")
hosts_units_info = hosts_units_info[hosts_units_info.unit_type == "Apartment"]
result = hosts_units_info.groupby(by="nationality").unit_id.nunique().to_frame(
'apartment_count').reset_index().sort_values('apartment_count')
result
